apiVersion: operator.istio.io/v1alpha1
kind: IstioOperator
spec:
  values:
    global:
      mtls:
        enabled: false
        auto: true
    # Requiring grafana to show the perf dashboard.
    grafana:
      enabled: true